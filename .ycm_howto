-install ycm plubin via Plug in VIM(:PlugInstall)
-install CMake, Python(if needed)
apt install build-essential cmake python3-dev -y
-install mono-complete(C#), golang(go), nodejs(node), jdk npm(node)
apt install mono-complete golang nodejs openjdk-17-jdk openjdk-17-jre npm
-complie ycm
cd ~/.vim/bundle/YouCompleteMe
python3 install.py --all
--go-completer
--java-complete
--rust-completer
--ts-completer
--cs-completer
